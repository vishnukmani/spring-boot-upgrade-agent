# application.yml
spring:
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-3.5-turbo
          temperature: 0.7
  main:
    banner-mode: off
logging:
  level:
    org.springframework: WARN
    root: WARN